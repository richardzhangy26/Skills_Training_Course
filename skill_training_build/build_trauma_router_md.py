#!/usr/bin/env python3
"""组装"急诊创伤处置（路由合并版）"训练剧本：路由阶段 + 助学分支(8阶段) + 考核分支(5阶段)。

来源：
- 助学阶段取自 训练剧本配置（助学分支）.md（阶段1-8 → 新编号2-9）
- 考核阶段取自 训练剧本配置.md（阶段1-5 → 新编号10-14）

变换：
- 数字人字段统一回填线上任务已验证的配置（customNid=KJ2VnNtYaa）
- 助学阶段背景图路径指向实际已生成的文件名
- 助学总结跳转关键词 训练完成 → 助学训练完成（与考核总结区分）
"""
import re
from pathlib import Path

BASE = Path("/Users/zhangyichi/工作/能力训练/skills_training_course/南方医科大学-临床技能模块")
ZHUXUE_MD = BASE / "训练剧本配置（助学分支）.md"
KAOHE_MD = BASE / "训练剧本配置.md"
OUT_MD = BASE / "训练剧本配置（路由合并版）.md"

DIGITAL_HUMAN = "KJ2VnNtYaa"
VOICE = "Tg2LpKo18D"
AVATAR = "c5Hzcef2jm"

ZHUXUE_BG = {
    1: "训练剧本配置（助学分支）_stage_1_手卫生（含醇手消毒液规范使用）.png",
    2: "训练剧本配置（助学分支）_stage_2_外科洗手（刷手范围与冲擦要点）.png",
    3: "训练剧本配置（助学分支）_stage_3_戴无菌手套（无菌原则与袖口检查）.png",
    4: "训练剧本配置（助学分支）_stage_4_消毒操作（方向、范围与次数）.png",
    5: "训练剧本配置（助学分支）_stage_5_常用消毒剂（上篇）——碘酊_酒精_碘伏.png",
    6: "训练剧本配置（助学分支）_stage_6_常用消毒剂（下篇）——苯扎溴铵_过氧化氢_松节油.png",
    7: "训练剧本配置（助学分支）_stage_7_铺单（顺序与移动原则）.png",
    8: "训练剧本配置（助学分支）_stage_8_助学总结复盘.png",
}


def extract_stage_chunks(text):
    """按 ### 阶段N 标题切分，返回 [(原编号, 正文)]，正文不含阶段间的 --- 分隔线。

    注意：不能用 ^## 通用匹配截断——提示词代码块内部就有 `## 步骤 0` 这类行。
    中间chunk以下一个 ### 阶段 为界；最后一个chunk以文件级 `## 🔄` 标题为界。
    """
    marks = [(m.start(), int(m.group(1))) for m in re.finditer(r"^### 阶段(\d+):[^\n]*$", text, re.M)]
    footer = re.search(r"^## 🔄", text, re.M)
    footer_pos = footer.start() if footer else len(text)
    chunks = []
    for i, (pos, num) in enumerate(marks):
        end = marks[i + 1][0] if i + 1 < len(marks) else footer_pos
        body = text[pos:end].rstrip()
        body = re.sub(r"\n+---\s*$", "", body).rstrip()
        chunks.append((num, body))
    return chunks


def apply_digital_human(body):
    body = re.sub(r"^\*\*数字人\*\*:.*$", f"**数字人**: {DIGITAL_HUMAN}", body, flags=re.M)
    body = re.sub(r"^\*\*声音\*\*:.*$", f"**声音**: {VOICE}", body, flags=re.M)
    body = re.sub(r"^\*\*形象\*\*:.*$", f"**形象**: {AVATAR}", body, flags=re.M)
    return body


def renumber(body, old_num, new_num):
    return body.replace(f"### 阶段{old_num}:", f"### 阶段{new_num}:", 1)


def transform_zhuxue(body, old_num, new_num):
    body = renumber(body, old_num, new_num)
    body = apply_digital_human(body)
    body = re.sub(
        r"^\*\*背景图\*\*:.*$",
        f"**背景图**: backgrounds/{ZHUXUE_BG[old_num]}",
        body,
        flags=re.M,
    )
    if old_num == 8:  # 助学总结：跳转关键词与收尾语调整
        body = body.replace("训练完成", "助学训练完成")
        body = body.replace(
            "通识知识是地基，后面到病例实战里，就能检验这些知识用得怎么样了。针对今天的内容，你还有什么疑问吗？如果没有，请回复'完成'以结束。",
            "以上就是这次助学训练的全部内容。如果想检验学习效果，可以再次进入本训练、在开头选择考核模式。针对今天的内容，你还有什么疑问吗？如果没有，请回复'完成'以结束。",
        )
    return body


def transform_kaohe(body, old_num, new_num):
    body = renumber(body, old_num, new_num)
    body = apply_digital_human(body)
    return body


HEADER = """# 急诊创伤处置——无菌技术实战训练（助学+考核双分支） - 训练剧本配置

## 📋 基础配置
- **任务名称**：急诊创伤处置——无菌技术实战训练
- **任务描述**：训练开头由李老师在路由阶段引导学生二选一：选"助学模式"——先以"讲解-提问-反馈"闭环学习无菌技术六大通识知识点，学完助学总结即结束训练；选"考核模式"——直接进入4个创伤病例的实战考核与总结复盘。两条分支平行独立，共用同一数字人李老师。
- **智能体角色**：李老师 - 急诊科资深带教老师
- **目标受众**：临床医学/护理相关专业学生（南方医科大学-临床技能模块）
- **背景图**: backgrounds/急诊创伤处置——无菌技术实战训练_cover.png

## 📝 训练阶段

### 阶段1: 训练模式选择

**虚拟训练官名字**: 李老师

**模型**: Doubao-Seed-1.6

**数字人**: KJ2VnNtYaa

**数字人名称**: 李老师

**声音**: Tg2LpKo18D

**形象**: c5Hzcef2jm

**阶段描述**: 李老师介绍助学模式与考核模式两种训练方式，请学生明确选择后分流到对应分支；选择不明确时澄清确认，绝不替学生做决定。

**背景图**: backgrounds/急诊创伤处置——无菌技术实战训练_cover.png

**互动轮次**: 6轮

**flowCondition**: "进入助学模式阶段"

**transitionPrompt**:
`
【输入参数】\\n
- 下一阶段原始开场白 ${next_stage_opening}\\n    \\n
【输出要求】\\n
直接以新阶段角色身份原样输出 ${next_stage_opening}，不做任何改写、扩写、续写、解释。\\n    \\n
【严禁】\\n
- 严禁输出"设计说明""设计亮点""小贴士"等元文本
- 严禁反问学生或追加新问题
- 严禁以上一阶段角色口吻续讲
- 严禁添加括号外的旁白、台词标注或markdown格式说明
`

**开场白**:
```
你好，我是李老师，急诊科带教。这次无菌技术训练开始前，先说清楚两种模式：一种是"助学模式"——我先带你把无菌技术的六大通识知识点过一遍，边讲边穿插小问题，答对答错都没关系，学完这次训练就结束了；另一种是"考核模式"——直接进入四个创伤病例的实战考核，我只追问、不讲解。你想选哪种？回复"助学"或"考核"就行。
```

**提示词**:
```
# Role

你现在扮演"急诊科资深带教老师——李老师"。

- **人设详情**：你是一位急诊科资深带教老师，语气温和、耐心引导。此刻你在训练入口，任务是帮助学生明确选择两种训练模式之一："助学模式"（先学通识知识点，学完即结束本次训练）或"考核模式"（直接进入病例实战考核）。

- **当前情境**：学生刚进入训练，尚未选择训练模式。

- **当前任务**：引导学生明确选择两种模式之一并分流。**严禁替学生做决定，严禁默认进入某一模式**。

# Opening Line (你已经在上一轮输出过这句话，请基于此进行回复)

"你好，我是李老师，急诊科带教。这次无菌技术训练开始前，先说清楚两种模式：一种是'助学模式'——我先带你把无菌技术的六大通识知识点过一遍，边讲边穿插小问题，答对答错都没关系，学完这次训练就结束了；另一种是'考核模式'——直接进入四个创伤病例的实战考核，我只追问、不讲解。你想选哪种？回复'助学'或'考核'就行。"

# Workflow & Interaction Rules

## 步骤 0：全案回溯与状态判定（核心）

在生成回复前，**请立即回读本轮及之前的所有对话记录**，判定学生的选择状态（**注意：以下提取的信息仅供你内部判断使用，严禁在回复中直接输出提取到的内容**）：

- **助学模式意向**：学生明确表达想先学习、学知识点、选助学（等类似意思），如"助学""我想先学习""先讲知识点""我先学一遍"（等类似意思）。
- **考核模式意向**：学生明确表达想直接考核、开始病例、直接考试（等类似意思），如"考核""直接考核""开始病例吧""我准备好了直接考"（等类似意思）。
- **不明确**：学生回答含糊（"随便""都行""不知道选哪个"），或同时涉及两种模式，或与选择无关的内容。

## 步骤 1：应答策略判定（分支互斥：每次仅执行一个分支，按以下优先级顺序判定）

### 分支 Z：兜底处理（无关话题/非法输入）【最高优先级】

- **判定依据**：学生输入与模式选择完全无关（如闲聊天气、询问私人问题、输入乱码，等类似意思），或无法识别为有效选择。
- **回复策略**：温柔地指出无关，拉回模式选择问题，一句话重申两种模式。
- **话术示例**："咱们先把训练模式定了哈。想让我先带你过一遍知识点，就说'助学'；想直接进病例考核，就说'考核'。"
  *(注意：不要完全按照话术实例输出，要根据学生的回答适当调整逻辑)*

### 分支 A：学生选择助学模式 → 跳转【次优先级】

- **判定依据**：学生明确选择助学模式（等类似意思）。
- **回复策略**：
  - **不要输出任何对话内容**。
  - **仅输出**跳转关键词：`进入助学模式阶段`
- **严禁**：任何标点、空格、寒暄（如"好的，我们开始学习"）

### 分支 B：学生选择考核模式 → 跳转

- **判定依据**：学生明确选择考核模式（等类似意思）。
- **回复策略**：
  - **不要输出任何对话内容**。
  - **仅输出**跳转关键词：`进入考核模式阶段`
- **严禁**：任何标点、空格、寒暄（如"好的，开始考核"）

### 分支 C：选择不明确 → 澄清确认

- **判定依据**：学生回答含糊（"随便""都行""听您的"）、同时涉及两种模式，或反问哪种更好（等类似意思）。
- **回复策略**：简要说明两种模式各自适合的情况，请学生给出明确选择。**严禁替学生做决定，严禁输出跳转关键词**。
- **话术示例**："这么说吧：无菌技术知识点还没把握、想先学一遍的，选助学模式；想直接检验自己水平的，选考核模式。这个得你来定——回复'助学'或者'考核'就行。"
  *(注意：不要完全按照话术实例输出，要根据学生的回答适当调整逻辑)*

# Response Constraints

1. **跳转纯净性**：学生明确选择后，**仅输出**对应跳转关键词（`进入助学模式阶段` 或 `进入考核模式阶段`），严禁输出标点、空格或额外文字。

2. **严禁替学生选择**：选择不明确时必须执行分支C澄清确认；学生未明确选择前，严禁输出任何跳转关键词，严禁默认进入某一模式。

3. **关键词一一对应**：助学模式对应 `进入助学模式阶段`，考核模式对应 `进入考核模式阶段`，严禁混用。

4. **【严禁逻辑外显】**：严禁在回复中出现任何"系统指令""逻辑判断说明"或思考过程，必须完全是李老师对学生说的话。

5. **字数与节奏**：澄清回复（分支C）限制在150字以内，简洁明了。

6. **对话自然性**：严禁在对话输出中使用"分支""路由""节点""默认边"等系统字样。
```
"""

FOOTER = """
---

## 🔄 阶段跳转关系
- 阶段1 → 阶段2：条件为路由阶段学生明确选择助学模式，跳转关键词 `进入助学模式阶段`（默认边）
- 阶段1 → 阶段10：条件为路由阶段学生明确选择考核模式，跳转关键词 `进入考核模式阶段`（非默认边，由布线脚本补建）
- 阶段2 → 阶段3：条件为手卫生互动反馈完成且学生确认，跳转关键词 `进入外科洗手阶段`
- 阶段3 → 阶段4：条件为外科洗手互动反馈完成且学生确认，跳转关键词 `进入戴无菌手套阶段`
- 阶段4 → 阶段5：条件为戴无菌手套互动反馈完成且学生确认，跳转关键词 `进入消毒操作阶段`
- 阶段5 → 阶段6：条件为消毒操作互动反馈完成且学生确认，跳转关键词 `进入消毒剂选择上篇阶段`
- 阶段6 → 阶段7：条件为消毒剂上篇互动反馈完成且学生确认，跳转关键词 `进入消毒剂选择下篇阶段`
- 阶段7 → 阶段8：条件为消毒剂下篇互动反馈完成且学生确认，跳转关键词 `进入铺单阶段`
- 阶段8 → 阶段9：条件为铺单互动反馈完成且学生确认，跳转关键词 `进入助学总结阶段`
- 阶段9 → 结束：条件为助学总结已输出 + 学生明确表示结束意图，跳转关键词 `助学训练完成`（非线性边，由布线脚本补建）
- 阶段10 → 阶段11：条件为上腹部清洁切口消毒12个必答维度全部完成，跳转关键词 `进入四肢清创阶段`
- 阶段11 → 阶段12：条件为四肢污染伤口清创消毒11个必答维度全部完成，跳转关键词 `进入面部创伤处理阶段`
- 阶段12 → 阶段13：条件为面部敏感部位消毒8个必答维度全部完成，跳转关键词 `进入动物咬伤处置阶段`
- 阶段13 → 阶段14：条件为动物咬伤综合处置9个必答维度全部完成，跳转关键词 `进入总结阶段`
- 阶段14 → 结束：条件为考核总结已输出 + 学生明确表示结束意图，跳转关键词 `训练完成`

## 📖 配置说明

- **拓扑结构**：路由中枢型双分支。阶段1为路由入口，双出边——助学分支（阶段2-9）与考核分支（阶段10-14）完全平行、互不衔接；学生选定后沿所在分支走到各自的结束边（助学分支输出 `助学训练完成`，考核分支输出 `训练完成`）。经教师确认：助学分支学完即结束训练，不再串接病例考核；想考核需重新进入训练选择考核模式。

- **路由设计**：路由阶段严禁替学生做决定——选择不明确时澄清确认，不输出跳转关键词；线性导入保留的"阶段1→阶段2（助学）"边为默认边，布线脚本补建"阶段1→阶段10（考核）"非默认边，并删除线性导入误连的"阶段9→阶段10"边、补建"阶段9→END"边。

- **数字人配置**：全部14个阶段统一复用线上任务已创建的"李老师"数字人（customNid=`KJ2VnNtYaa`，灿灿音色`Tg2LpKo18D`，形象`c5Hzcef2jm`），与既有考核任务完全一致；数字人字段填写customNid走精确匹配，避免声音+形象回退匹配误复用。

- **内容来源**：助学分支（阶段2-9）来自《训练剧本配置（助学分支）.md》——"讲解-提问-反馈"闭环、一答一反馈、温和纠错；考核分支（阶段10-14）来自《训练剧本配置.md》——苏格拉底式查缺补漏追问、逐项通关。两条分支提示词均原样保留，仅调整阶段编号、数字人字段与背景图路径。

- **背景图说明**：助学分支8张背景图与双封面已生成（backgrounds/训练剧本配置（助学分支）_*.png），本配置已指向实际文件名；考核分支沿用原绝对路径背景图；路由阶段复用考核任务封面图。

- **评分标准使用说明**：考核分支沿用源文档《临床技能模块》审阅修改版.docx的4×25分评价体系；助学分支以学习巩固为目标，不设独立评分，总结阶段复盘报告中的实际答题表现可作为参考。
"""


def main():
    zhuxue_text = ZHUXUE_MD.read_text(encoding="utf-8")
    kaohe_text = KAOHE_MD.read_text(encoding="utf-8")

    zhuxue_chunks = extract_stage_chunks(zhuxue_text)
    kaohe_chunks = extract_stage_chunks(kaohe_text)
    assert len(zhuxue_chunks) == 8, f"助学阶段数异常: {len(zhuxue_chunks)}"
    assert len(kaohe_chunks) == 5, f"考核阶段数异常: {len(kaohe_chunks)}"

    parts = [HEADER.rstrip()]
    for old_num, body in zhuxue_chunks:
        new_num = old_num + 1
        parts.append("\n\n---\n\n" + transform_zhuxue(body, old_num, new_num))
    for old_num, body in kaohe_chunks:
        new_num = old_num + 9
        parts.append("\n\n---\n\n" + transform_kaohe(body, old_num, new_num))
    parts.append(FOOTER.rstrip() + "\n")

    out = "".join(parts)

    # 一致性断言
    for kw in ["进入助学模式阶段", "进入考核模式阶段", "助学训练完成", "训练完成"]:
        n = out.count(kw)
        print(f"关键词 {kw!r}: {n} 处")
    assert out.count("进入助学模式阶段") >= 4
    assert out.count("进入考核模式阶段") >= 4
    assert "**数字人**: KJ2VnNtYaa" in out
    assert out.count("**数字人**: KJ2VnNtYaa") == 14, f"数字人回填数量异常: {out.count('**数字人**: KJ2VnNtYaa')}"
    assert re.search(r"^### 阶段14:", out, re.M), "缺少阶段14"

    OUT_MD.write_text(out, encoding="utf-8")
    print(f"✅ 已写入 {OUT_MD}（{len(out)} 字符）")


if __name__ == "__main__":
    main()
